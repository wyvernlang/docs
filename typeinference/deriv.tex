\inferrule*[right=$\rightarrow$E]{
	\inferrule*[right=$\rightarrow$E]{
		\inferrule*[right=Var]
			{(\text{pickOne} : \forall \alpha.\alpha\rightarrow\alpha\rightarrow\alpha) \in \Gamma}
			{\Gamma\vdash \text{pickOne} \synth \alpha.\alpha\rightarrow\alpha\rightarrow\alpha\dashv\Gamma}\\
		\inferrule*[right=$\forall$App]{
			\inferrule*[right=$\rightarrow$App]{
				\inferrule*[]{
					\inferrule*[right=Intro]{ }{\Gamma \vdash \intro{Cat} \synth Cat \dashv \Gamma}\\
					\inferrule*[]{
						\inferrule*[]{
							\Gamma_u \vdash Cat\\
							\inferrule*[right=BndRTop]{ }{\Gamma_u \vdash Cat \sigbndr \pair{\{\}}{\top} \dashv \Gamma_u}\\
							\inferrule*[right=]{ }{\Gamma_u \vdash \pair{\{\}}{\bot} \glb Cat = \pair{\{\}}{Cat}}
						}{\Gamma_u,\bound{\pair{\{\}}{\bot}}{\alphahat}{\pair{\{\}}{\top}},\Gamma_l \vdash Cat \instr \alphahat \dashv \Gamma_u,\bound{\pair{\{\}}{\bot}}{\alphahat}{\pair{\{\}}{Cat}},\Gamma_l}}
					{\Gamma \vdash Cat \st \alphahat \dashv TODO}
				}{\Gamma \vdash \intro{Cat} \analyzes \alphahat \dashv TODO}
			}{\Gamma \vdash \alphahat\rightarrow(\alphahat\rightarrow\alphahat) \bullet \intro{Cat} \fnSynth TODO}
		}{
			\Gamma \vdash \forall \alpha.\alpha\rightarrow\alpha\rightarrow\alpha \bullet \intro{Cat} \fnSynth TODO
		}
	}{
		\Gamma \vdash \text{pickOne} \intro{Cat} \synth
	}

}{ \Gamma \vdash (\text{pickOne} \intro{Cat})(\intro{Dog}) \dashv \Delta}