\begin{figure}[h]
\hfill \fbox{$\Gamma \vdash T \;  \textbf{wf}$}
\begin{mathpar}
\inferrule
  {\Gamma \vdash p \ni \texttt{type} \; L : S .. U \\
  	\Gamma \vdash \texttt{type} \; L : S .. U \; \textbf{wf}}
  {\Gamma \vdash p.L \; \textbf{wf}}
  \quad (\textsc {WF-Sel})
	\and
\inferrule
  {\forall \sigma_i \in \overline{\sigma}, \; \Gamma,\texttt{z}:\{\texttt{z} \Rightarrow \overline{\sigma}\} \vdash \sigma_i \; \textbf{wf} \\
  	\forall j \neq i, \; dom(\sigma_j) \neq dom(\sigma_i)}
  {\Gamma \vdash \{\texttt{z} \Rightarrow \overline{\sigma}\} \; \textbf{wf}}
  \quad (\textsc {WF-Rec})
%	\and
%\inferrule
%  {\Gamma \vdash S \;  \textbf{wf} \\
%  	\Gamma \vdash T \;  \textbf{wf}}
%  {\Gamma \vdash S \rightarrow T \;  \textbf{wf}}
%  \quad (\textsc {WF-Func})
	\and
\inferrule
  {}
  {\Gamma \vdash \top \;  \textbf{wf}}
  \quad (\textsc {WF-Top})
	\and
\inferrule
  {}
  {\Gamma \vdash \bot \;  \textbf{wf}}
  \quad (\textsc {WF-Bot})
\end{mathpar}
\hfill \fbox{$\Gamma \vdash \sigma \;  \textbf{wf}$}
\begin{mathpar}
\inferrule
  {\Gamma \vdash T : \textbf{wf}}
  {\Gamma \vdash \texttt{var} \; f:T \;  \textbf{wf}}
  \quad (\textsc {WF-Var})
	\and
\inferrule
  {\Gamma \vdash T : \textbf{wf} \\
  	\Gamma \vdash S : \textbf{wf}}
  {\Gamma \vdash \texttt{def} \; m:S \rightarrow T \;  \textbf{wf}}
  \quad (\textsc {WF-Def})
	\and
\inferrule
  {\Gamma \vdash S : \textbf{wf} \\
  	\Gamma \vdash U : \textbf{wf} \\
  	\Gamma \vdash S <: U}
  {\Gamma \vdash \texttt{type} \; L : S .. U \; \textbf{wf}}
  \quad (\textsc {WF-Type})
\end{mathpar}
\hfill \fbox{$\Gamma \;  \textbf{wf}$}
\begin{mathpar}
\inferrule
  {\forall x:T \in \Gamma, \;\; 	\Gamma \vdash T : \textbf{wf}}
  {\Gamma \;  \textbf{wf}}
  \quad (\textsc {WF-Context})
\end{mathpar}
%\hfill \fbox{$\Sigma \;  \textbf{wf}$}
%\begin{mathpar}
%\inferrule
%  {\forall l:\{\texttt{z} \Rightarrow \overline{\sigma}\} \in \Sigma, \;\;
%  	\varnothing \vdash \{\texttt{z} \Rightarrow \overline{\sigma}\} : \textbf{wf}}
%  {\Sigma \;  \textbf{wf}}
%  \quad (\textsc {WF-Store-Context})
%\end{mathpar}
%\hfill \fbox{$\Sigma; \; \Gamma \vdash \Delta \;  \textbf{wf}$}
%\begin{mathpar}
%\inferrule
%  {\forall x \mapsto l \in \Delta, \; \Gamma \vdash \Sigma(l) <: \Gamma(x)}
%  {\Sigma; \; \Gamma \vdash \Delta \;  \textbf{wf}}
%  \quad (\textsc {WF-Subst-Context})
%\end{mathpar}
\caption{Well-Formedness}
\label{f:subtype}
\end{figure}