\begin{figure}[h]
\hfill \fbox{$\Gamma; E \vdash e:T$}
\begin{mathpar}
\inferrule
  {x : T \in \Gamma}
  {	\Gamma; E \vdash x : T}
  \quad (\textsc {T-Var})
	\and
\inferrule
  {\forall d_i \in \overline{d}, \; 
  \Gamma, \texttt{z} : \{\texttt{z} \Rightarrow \overline{\sigma}\}; 
  E[\texttt{var} \; y = \texttt{new} \; \{\texttt{z} \Rightarrow ..., D_i[\bigcirc], ...\}; \; e] 
  \vdash d_i : \sigma_i\\
  	\Gamma, y : \{\texttt{z} \Rightarrow \overline{\sigma}\}; 
  	E[\texttt{var} \; y = \texttt{new} \; \{\texttt{z} \Rightarrow \overline{d}\}; \; \bigcirc]
  	\vdash e : T \\
  	y \notin \Gamma}
  {	\Gamma; E \vdash \texttt{var} \; y = \texttt{new} \; \{\texttt{z} \Rightarrow \overline{d}\}; \; e : T}
  \quad (\textsc {T-New})
	\and
\inferrule
  {	\Gamma; E[e_0.m(\bigcirc)] \vdash e_1 : S \\
  	\Gamma; E[\bigcirc.m(e_1)] \vdash e_0 : T_0 \\
  	T_0 \ni \texttt{def} \; m:S \rightarrow T}
  {	\Gamma; E \vdash e_0.m(e_1) : T}
  \quad (\textsc {T-Meth})
	\and
\inferrule
  {	\Gamma; E[\bigcirc.f] \vdash e_0 : T_0 \\
  	T_0 \ni \texttt{def} \; f:T = e}
  {	\Gamma; E \vdash e_0.f : T}
  \quad (\textsc {T-Acc})
%	\and
%\inferrule
%  {	\Gamma; E \vdash e_0 : T_0 \\
%  	T_0 \ni \texttt{def} \; f:T = e \\
%  	\Gamma; E \vdash e_1 : T}
%  {	\Gamma; E \vdash e_0.f = e_1 : T}
%  \quad (\textsc {T-Assign})
%	\and
%\inferrule
%  {	l:T \in \Sigma}
%  {	\Gamma \vdash l : T}
%  \quad (\textsc {T-Loc})
	\and
\inferrule
  {	\Gamma; E[\bigcirc \unlhd T] \vdash e : S \\
  	\Gamma; E[\bigcirc \unlhd T] \vdash S <: T}
  {	\Gamma; E \vdash e \unlhd T : T}
  \quad (\textsc {T-Wide})
	\and
\inferrule
  {	\Gamma; E \vdash e : S \\
  	\Gamma; E \vdash S <: T}
  {	\Gamma; E \vdash e : T}
  \quad (\textsc {T-Sub})
\end{mathpar}
\caption{Expression Typing}
\label{f:e_typ}
\end{figure}
\begin{figure}[h]
\hfill \fbox{$\Gamma; E \vdash d:\sigma$}
\begin{mathpar}
\inferrule
  {\Gamma; E \vdash e : T}
  {\Gamma; E \vdash \texttt{def} \; f:T = e : \texttt{def} \; f:T}
  \quad (\textsc {T-Decl-Var})
	\and
\inferrule
  {\Gamma, x : S; E \vdash e_0 : T}
  {\Gamma; E \vdash \texttt{def} \; m:T(x:S) = e : \texttt{def} \; m:S \rightarrow T}
  \quad (\textsc {T-Decl-Def})
	\and
\inferrule
  {\Gamma; E \vdash \texttt{type} \; L : S .. U \; \textbf{wf} }
  {\Gamma; E \vdash \texttt{type} \; L : S .. U : \texttt{type} \; L : S .. U}
  \quad (\textsc {T-Decl-Type})
\end{mathpar}
\caption{Declaration Typing}
\label{f:d_typ}
\end{figure}
%\begin{figure}[h]
%\hfill \fbox{$\Gamma \vdash \mu:\Sigma$}
%\begin{mathpar}
%\inferrule
%  {\forall l \in dom(\mu), \; \mu(l)= \{\texttt{z} \Rightarrow \overline{d}\} \\
%  	\Sigma(l)= \{\texttt{z} \Rightarrow \overline{\sigma}\} \\
%  	\Gamma \vdash \overline{d} : \overline{\sigma}}
%  {\Gamma \vdash \mu : \Sigma}
%  \quad (\textsc {T-Store})
%\end{mathpar}
%\caption{Store Typing}
%\label{f:s_typ}
%\end{figure}