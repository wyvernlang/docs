\begin{figure}[h]
\hfill \fbox{$\Gamma; E \vdash e:T$}
\begin{mathpar}
\inferrule
  {x : T \in \Gamma}
  {	\Gamma \vdash x : T}
  \quad (\textsc {T-Var})
	\and
\inferrule
  {\forall d_i \in \overline{d}, \; 
  \Gamma, z : \{z \Rightarrow \overline{\sigma}\} 
  \vdash d_i : \sigma_i\\
%  	\Gamma, y : \{y \Rightarrow \overline{\sigma}\}
%  	\vdash e : T \\
  	y \notin dom(\Gamma)}
  {	\Gamma \vdash \texttt{var} \; z = \texttt{new} \; \{z \Rightarrow \overline{d}\} : 
  \{z \Rightarrow \overline{\sigma}\}}
  \quad (\textsc {T-New})
	\and
\inferrule
  {	\Gamma \vdash e_1 : S \\
  	\Gamma \vdash e_0 : T_0 \\
  	T_0 \ni \texttt{def} \; m:S \rightarrow T \\
  	\Gamma \vdash T <: U}
  {	\Gamma \vdash e_0.m_U(e_1) : U}
  \quad (\textsc {T-Meth})
	\and
\inferrule
  {	\Gamma \vdash e_0 : T_0 \\
  	T_0 \ni \texttt{def} \; f:T = e}
  {	\Gamma \vdash e_0.f : T}
  \quad (\textsc {T-Acc})
%	\and
%\inferrule
%  {	\Gamma; E \vdash e_0 : T_0 \\
%  	T_0 \ni \texttt{def} \; f:T = e \\
%  	\Gamma; E \vdash e_1 : T}
%  {	\Gamma; E \vdash e_0.f = e_1 : T}
%  \quad (\textsc {T-Assign})
%	\and
%\inferrule
%  {	l:T \in \Sigma}
%  {	\Gamma \vdash l : T}
%  \quad (\textsc {T-Loc})
	\and
\inferrule
  {	\Gamma \vdash e : S \\
  	\Gamma \vdash S <: T}
  {	\Gamma \vdash e \unlhd T : T}
  \quad (\textsc {T-Wide})
	\and
\inferrule
  {	\Gamma \vdash e : S \\
  	\Gamma \vdash S <: T}
  {	\Gamma \vdash e : T}
  \quad (\textsc {T-Sub})
\end{mathpar}
\caption{Expression Typing}
\label{f:e_typ}
\end{figure}
\begin{figure}[h]
\hfill \fbox{$\Gamma; E \vdash d:\sigma$}
\begin{mathpar}
\inferrule
  {\Gamma \vdash e : T}
  {\Gamma \vdash \texttt{def} \; f:T = e : \texttt{def} \; f:T}
  \quad (\textsc {T-Decl-Var})
	\and
\inferrule
  {\Gamma, x : S \vdash e_0 : T}
  {\Gamma \vdash \texttt{def} \; m(x:S) = e : T : \texttt{def} \; m:S \rightarrow T}
  \quad (\textsc {T-Decl-Def})
	\and
\inferrule
  {\Gamma \vdash \texttt{type} \; L : S .. U \; \textbf{wf} }
  {\Gamma \vdash \texttt{type} \; L : S .. U : \texttt{type} \; L : S .. U}
  \quad (\textsc {T-Decl-Type})
\end{mathpar}
\caption{Declaration Typing}
\label{f:d_typ}
\end{figure}
%\begin{figure}[h]
%\hfill \fbox{$\Gamma \vdash \mu:\Sigma$}
%\begin{mathpar}
%\inferrule
%  {\forall l \in dom(\mu), \; \mu(l)= \{\texttt{z} \Rightarrow \overline{d}\} \\
%  	\Sigma(l)= \{\texttt{z} \Rightarrow \overline{\sigma}\} \\
%  	\Gamma \vdash \overline{d} : \overline{\sigma}}
%  {\Gamma \vdash \mu : \Sigma}
%  \quad (\textsc {T-Store})
%\end{mathpar}
%\caption{Store Typing}
%\label{f:s_typ}
%\end{figure}