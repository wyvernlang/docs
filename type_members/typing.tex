\begin{figure}[h]
\hfill \fbox{$\Gamma; K \vdash e:T$}
\begin{mathpar}
\inferrule
  {x : T \in \Gamma}
  {	\Gamma; K \vdash x : T}
  \quad (\textsc {T-Var})
	\and
\inferrule
  {\Gamma, \texttt{self} : \{\texttt{self} \Rightarrow \overline{\sigma}\}; K \vdash \overline{d} : \overline{\sigma}\\
  	\Gamma, y : \{\texttt{self} \Rightarrow \overline{\sigma}\}; 
  	K,  (\texttt{var} \; y = \texttt{new} \; \{\texttt{self} \Rightarrow \overline{d}\}; \; \bigcirc)
  	\vdash e : T \\
  	y \notin \Gamma}
  {	\Gamma; K \vdash \texttt{var} \; y = \texttt{new} \; \{\texttt{self} \Rightarrow \overline{d}\}; \; e : T}
  \quad (\textsc {T-New})
	\and
\inferrule
  {	\Gamma; K, (e_0.m(\bigcirc)) \vdash e_1 : S \\
  	\Gamma; K, (\bigcirc.m(e_1)) \vdash e_0 : T_0 \\
  	T_0 \ni \texttt{def} \; m:S \rightarrow T}
  {	\Gamma; K \vdash e_0.m(e_1) : T}
  \quad (\textsc {T-Meth})
	\and
\inferrule
  {	\Gamma; K, (\bigcirc.f) \vdash e_0 : T_0 \\
  	T_0 \ni \texttt{def} \; f:T = e}
  {	\Gamma; K \vdash e_0.f : T}
  \quad (\textsc {T-Acc})
%	\and
%\inferrule
%  {	\Gamma; K \vdash e_0 : T_0 \\
%  	T_0 \ni \texttt{def} \; f:T = e \\
%  	\Gamma; K \vdash e_1 : T}
%  {	\Gamma; K \vdash e_0.f = e_1 : T}
%  \quad (\textsc {T-Assign})
%	\and
%\inferrule
%  {	l:T \in \Sigma}
%  {	\Gamma \vdash l : T}
%  \quad (\textsc {T-Loc})
	\and
\inferrule
  {	\Gamma; K, (\bigcirc \unlhd T) \vdash e : S \\
  	\Gamma; K, (\bigcirc \unlhd T) \vdash S <: T}
  {	\Gamma; K \vdash e \unlhd T : T}
  \quad (\textsc {T-Wide})
	\and
\inferrule
  {	\Gamma; K \vdash e : S \\
  	\Gamma; K \vdash S <: T}
  {	\Gamma; K \vdash e : T}
  \quad (\textsc {T-Sub})
\end{mathpar}
\caption{Expression Typing}
\label{f:e_typ}
\end{figure}
\begin{figure}[h]
\hfill \fbox{$\Gamma; K \vdash d:\sigma$}
\begin{mathpar}
\inferrule
  {\Gamma; K \vdash e : T}
  {\Gamma; K \vdash \texttt{def} \; f:T = e : \texttt{def} \; f:T}
  \quad (\textsc {T-Decl-Var})
	\and
\inferrule
  {\Gamma, x : S; K \vdash e_0 : T}
  {\Gamma; K \vdash \texttt{def} \; m:T(x:S) = e : \texttt{def} \; m:S \rightarrow T}
  \quad (\textsc {T-Decl-Def})
	\and
\inferrule
  {\Gamma; K \vdash \texttt{type} \; L : S .. U \; \textbf{wf} }
  {\Gamma; K \vdash \texttt{type} \; L : S .. U : \texttt{type} \; L : S .. U}
  \quad (\textsc {T-Decl-Type})
\end{mathpar}
\caption{Declaration Typing}
\label{f:d_typ}
\end{figure}
%\begin{figure}[h]
%\hfill \fbox{$\Gamma \vdash \mu:\Sigma$}
%\begin{mathpar}
%\inferrule
%  {\forall l \in dom(\mu), \; \mu(l)= \{\texttt{self} \Rightarrow \overline{d}\} \\
%  	\Sigma(l)= \{\texttt{self} \Rightarrow \overline{\sigma}\} \\
%  	\Gamma \vdash \overline{d} : \overline{\sigma}}
%  {\Gamma \vdash \mu : \Sigma}
%  \quad (\textsc {T-Store})
%\end{mathpar}
%\caption{Store Typing}
%\label{f:s_typ}
%\end{figure}