Wyvern module system theorems

	fundamental property:
		a module can only do something dangerous if I pass it a capability to do so
		
	special case: safe imports
		a module that imports only non-resource modules cannot access any external resources
		
	